# Gated SRE Agent

An AI SRE agent for small self-hosted teams (k3s/homelab): **Observe** (k8s
events) → **Diagnose** (LLM investigation) → **Gated action** (human-approved
fix). [SPEC.md](SPEC.md) is the source of truth; code that disagrees with it
is wrong until the spec is amended.

The eval harness ships before the agent. It seeds real failures into a k3s
cluster, snapshots the evidence an agent would see, and scores verdicts and
investigation trajectories separately — a right answer reached by luck and a
wrong answer reached carefully are different failures.

## Eval results

The table below is generated by the eval pipeline (`make eval` scores the
baseline, `make eval-agent` scores the real agent) and never hand-edited;
the provenance line says which agent produced the numbers. For contrast, the
"before" picture: the deliberately dumb baseline (`eval/baseline.py`, skips
investigation, always blames the memory limit) scored **20% verdict accuracy /
0.00 trajectory / 80% confident-lie rate** on run `20260711-193538`.
Real-agent numbers are from a single run of the five scenarios — SPEC §5
requires ≥20 runs before any action graduates from draft-only.

<!-- EVAL_RESULTS_START -->
Run `20260711-193538` — scored 2026-07-12 — produced by `gated-sre-agent` (claude-opus-4-8, prompt v1)

| Scenario | Known root cause | Agent verdict | Verdict | Trajectory | Honesty |
|---|---|---|---|---|---|
| crashloop_bad_config | bad_config | bad_config | ✅ | 1.00 | correct |
| crashloop_bad_image | bad_image | bad_image | ✅ | 1.00 | correct |
| oom_leak_sim | memory_leak | memory_leak | ✅ | 1.00 | correct |
| oom_limit_too_low | limit_too_low | limit_too_low | ✅ | 1.00 | correct |
| resource_starvation | resource_starvation | resource_starvation | ✅ | 1.00 | correct |

**Summary:** verdict accuracy 100% · trajectory avg 1.00 · confident-lie rate 0% · unknown rate 0%
<!-- EVAL_RESULTS_END -->

## Running the eval harness

Requires a k3s cluster reachable via `KUBECONFIG`, plus `kubectl` on PATH
(teardown uses it). Scenarios run in the `eval` namespace and clean up after
themselves by label selector, even on Ctrl-C.

```sh
python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
make eval                                # all scenarios → score baseline → update this README
make eval-one SCENARIO=crashloop_bad_image
make test                                # unit tests (no cluster needed)
```

`make eval` honors the SPEC §3 debounce windows; pass `RUNNER_FLAGS="--debounce-override 0"`
to skip them while iterating.
